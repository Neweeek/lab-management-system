# 实验室成员与工位管理系统——开发需求文档

> 本文档用于交给 Codex 等编码代理实施。请先阅读完整文档，再进行项目设计与编码；不要擅自扩大第一版范围。遇到未明确的关键业务规则时，先向用户提问。

## 1. 项目背景

创新创业竞赛实验室采用工位制度，但目前存在以下问题：

- 固定工位数量有限，部分成员长期不来实验室却持续占用工位；
- 一些积极、有能力、经常参与实验室活动的学生无法获得工位；
- 成员身份、固定工位资格和项目参与情况混在一起，缺少动态调整机制；
- 管理员缺少连续、可靠的依据判断是否保留某位成员的固定工位；
- 不希望采用复杂积分制或单纯签到制，以免产生挂机、刷数据和形式主义。

本系统的定位不是普通考勤系统，而是：

> **实验室成员与稀缺资源治理系统。**

第一版仅围绕“成员、工位、考核、周报和人工决策”建设，后续才考虑门禁、项目管理、设备借用、Git 仓库和 AI 分析等扩展。

## 2. 核心原则

1. **成员身份与工位资格分离。**失去固定工位不等于退出实验室。
2. 系统中统一使用“固定工位”，不使用“永久工位”。固定工位仍可依规回收。
3. 普通成员平时不需要每周提交报告；只有进入考核期后才产生周报任务。
4. 系统负责保存证据、整理信息和提醒管理员，最终决定必须由管理员作出。
5. 不建立“低于某分自动取消工位”的综合评分机制。
6. 允许成员合理使用 AI，但成员必须能够解释、修改、调试和维护自己提交的成果。
7. 考核关注有效投入、成长、协作和资源利用情况，不以性格内向、当前技术基础弱等作为负面依据。

## 3. 推荐技术栈

以下为默认方案，开始编码前如现有项目技术栈不同，应先询问用户：

- 后端：Java 21、Spring Boot 3、Spring Security、JWT、MyBatis-Plus；
- 前端：Vue 3、TypeScript、Vite、Element Plus、Pinia、Vue Router；
- 数据库：MySQL 8；
- 接口：RESTful API；
- 文件：第一版允许保存附件 URL 或本地文件路径，文件存储方式做成可替换配置；
- 部署：前后端分离，Nginx 反向代理。

## 4. 用户角色与权限

### 4.1 普通成员

- 登录、查看和修改有限的个人资料；
- 查看实验室工位平面图与工位状态；
- 选择空闲工位并提交固定工位申请；
- 查看自己的申请、固定工位和历史变更记录；
- 查看自己是否处于考核期以及考核起止时间、原因和要求；
- 提交、暂存和查看自己的考核周报；
- 登记特殊情况，例如考试、保研、实习、竞赛或生病；
- 查看管理员对本人考核作出的最终决定。

### 4.2 管理员

- 管理成员、角色、成员状态和工位；
- 审核或拒绝工位申请；
- 为成员分配、调整或释放固定工位；
- 发起、延长或提前结束考核；
- 查看考核期内的周报、工位数据、项目/活动信息及特殊情况；
- 在考核结束时进行人工决策；
- 查看全部审计记录。

第一版只设置 `MEMBER` 和 `ADMIN` 两类权限。后续可增加指导教师或只读审核者。

## 5. 状态设计

### 5.1 成员状态

| 状态 | 说明 |
|---|---|
| `OBSERVATION` | 观察成员，通常为新招成员 |
| `ACTIVE` | 正式成员，可申请固定工位 |
| `MOBILE` | 流动成员，无固定工位，可使用临时/公共工位 |
| `SUSPENDED` | 暂停活动，保留档案但不占固定工位 |
| `EXITED` | 已退出实验室，禁止继续申请工位 |

### 5.2 工位状态

| 状态 | 说明 |
|---|---|
| `AVAILABLE` | 空闲，可申请 |
| `PENDING` | 已被申请，等待管理员处理 |
| `OCCUPIED` | 已分配为固定工位 |
| `TEMPORARY` | 公共或临时工位，不固定绑定某一成员 |
| `DISABLED` | 维修、停用或不可分配 |

### 5.3 工位申请状态

`PENDING → APPROVED / REJECTED / CANCELLED`

约束：

- 同一成员同一时间最多存在一个待审核工位申请；
- 已拥有固定工位的成员不能再次申请；
- 工位被申请后进入 `PENDING`，避免多人同时占用；
- 审核通过后工位为 `OCCUPIED`，申请人与工位建立当前绑定关系；
- 拒绝或取消后，如无其他有效申请，工位恢复 `AVAILABLE`；
- 审核操作必须使用事务与并发控制，不能把同一工位分配给多人。

### 5.4 考核状态

| 状态 | 说明 |
|---|---|
| `PLANNED` | 已创建，尚未开始 |
| `ACTIVE` | 考核进行中 |
| `AWAITING_DECISION` | 已到期，等待管理员决策 |
| `COMPLETED` | 已作出最终决定 |
| `CANCELLED` | 已取消 |

### 5.5 考核结果

- `KEEP_SEAT`：保留固定工位；
- `EXTEND_REVIEW`：延长考核；
- `MOVE_TO_MOBILE`：转为流动成员并释放固定工位；
- `EXEMPTED`：因特殊情况豁免，本周期不作负面调整。

“退出实验室”不作为普通考核的一键结果。若管理员需要将成员设为 `EXITED`，必须通过单独的成员状态变更操作并填写原因。

## 6. 核心业务流程

### 6.1 工位申请与分配

1. 普通成员打开工位平面图；
2. 选择一个 `AVAILABLE` 工位，查看编号、区域和备注；
3. 填写简短申请理由并提交；
4. 工位暂时变为 `PENDING`；
5. 管理员审核：
   - 通过：工位变为 `OCCUPIED`，创建成员—工位绑定记录；
   - 拒绝：填写原因，工位恢复 `AVAILABLE`；
6. 成员可在个人中心查看申请与工位变更历史。

### 6.2 发起考核

管理员选择成员并填写：

- 考核原因；
- 开始时间和结束时间；
- 周报提交周期，第一版固定为每周一次；
- 每周截止时间；
- 可选的补充要求和备注。

系统根据考核周期自动生成周报任务。示例：考核 4 周，则生成 4 个独立周报任务。

### 6.3 考核期周报

周报必须为结构化表单，不能只有一个大文本框。建议字段：

- 本周主要工作；
- 参与的项目或竞赛；
- 已完成的具体任务；
- 学习内容与技术成长；
- 阅读的论文、资料或开源项目；
- 代码、文档、演示或其他成果链接；
- 遇到的问题以及采取的解决方案；
- 与实验室相关的协作、分享或支持；
- 下周计划；
- 本周到实验室的大致日期或次数（由本人陈述，不能直接视为客观考勤）；
- 其他说明；
- 可选附件。

周报状态：`DRAFT / SUBMITTED / LATE_SUBMITTED / MISSED`。

提交规则：

- 截止前可反复保存草稿；
- 正式提交后原则上不可直接覆盖，若允许修改，应保留版本或修改记录；
- 截止后仍允许补交，但标记为 `LATE_SUBMITTED`；
- 超期未交由定时任务标记为 `MISSED`；
- 不允许成员填写其他成员的周报。

### 6.4 特殊情况登记

成员可登记起止日期、类型、说明和可选证明附件。类型包括：

- 考试或保研准备；
- 实习；
- 学科竞赛；
- 科研任务；
- 生病或个人紧急情况；
- 其他。

特殊情况仅作为管理员判断依据，不自动免除考核，也不自动保留工位。

### 6.5 到期汇总与人工决策

考核结束时间到达后：

1. 系统将考核状态切换为 `AWAITING_DECISION`；
2. 生成可读的考核汇总页；
3. 管理员查看全部证据；
4. 管理员选择结果并填写决策说明；
5. 系统执行对应状态变化并记录审计日志。

汇总页至少展示：

- 成员基本信息、当前成员状态与固定工位；
- 考核原因、周期和要求；
- 应交/已交/迟交/缺交周报数量；
- 各周报告时间线和结构化内容；
- 周报内提到的项目、成果链接、问题和计划；
- 工位使用或活动记录（第一版若无客观数据，则明确显示“暂无客观数据”）；
- 特殊情况记录；
- 管理员补充记录；
- 历史考核与历史工位变更；
- 决策操作区。

系统可做事实性统计，但不得自动给出综合分数或自动执行取消工位决定。

### 6.6 决策后的状态变化

- `KEEP_SEAT`：成员和工位状态不变，考核完成；
- `EXTEND_REVIEW`：结束当前考核并创建新的延长周期，或在保留完整历史的前提下延长截止时间；
- `MOVE_TO_MOBILE`：成员变为 `MOBILE`，当前固定工位释放为 `AVAILABLE`；
- `EXEMPTED`：工位与成员状态保持不变，保存豁免原因。

任何会释放工位的操作都必须在同一事务内完成：关闭绑定关系、修改成员状态、更新工位状态、写入审计日志。

## 7. 第一版功能模块

### 7.1 登录与权限

- 账号密码登录、退出；
- JWT 鉴权；
- 前后端路由和接口权限控制；
- 管理员创建成员账号或导入初始账号；
- 密码加密保存，禁止明文存储。

### 7.2 成员管理

- 成员列表、详情、搜索和筛选；
- 维护姓名、学号、年级、专业、联系方式、技术方向、加入日期；
- 修改成员状态并填写原因；
- 查看固定工位、申请、考核、周报、特殊情况和操作历史。

### 7.3 工位管理

- 管理区域和工位编号；
- 平面图/网格方式展示工位；
- 颜色区分空闲、待审核、已占用、临时和停用；
- 普通成员选择并申请；
- 管理员审核、直接分配、调整和释放；
- 记录每次绑定和解除的时间及原因。

第一版平面图不要求复杂拖拽编辑器，可使用按区域分组的二维网格。

### 7.4 考核管理

- 发起考核、查看进行中和历史考核；
- 自动生成周报任务；
- 截止提醒和逾期标记；
- 到期后生成汇总；
- 管理员作出并保存决定。

### 7.5 个人中心

- 我的资料；
- 我的工位及申请；
- 我的考核；
- 我的周报任务；
- 我的特殊情况；
- 消息/待办提示。

### 7.6 审计日志

以下操作必须记录操作者、目标、操作时间、操作前后状态和原因：

- 工位申请审核；
- 工位分配、调整、释放；
- 发起、延长、取消考核；
- 考核决策；
- 成员状态变化；
- 管理员修改成员周报或档案（如果允许）。

## 8. 建议数据模型

具体字段名可按项目规范调整，但不要把成员状态和工位状态混成一个字段。

| 表 | 主要用途 |
|---|---|
| `users` | 登录账号、密码摘要、角色、账号状态 |
| `member_profiles` | 姓名、学号、年级、专业、方向、成员状态等 |
| `seat_areas` | 工位区域 |
| `seats` | 工位编号、坐标、状态、备注 |
| `seat_applications` | 工位申请、理由、审核人、结果和审核意见 |
| `seat_assignments` | 成员与固定工位绑定历史，含开始、结束及解除原因 |
| `reviews` | 考核原因、周期、状态、发起人和最终结果 |
| `weekly_report_tasks` | 某次考核自动生成的每周报告任务和截止时间 |
| `weekly_reports` | 结构化周报内容、提交状态和提交时间 |
| `report_attachments` | 周报附件或成果链接 |
| `special_circumstances` | 特殊情况类型、起止时间、说明和附件 |
| `admin_notes` | 管理员对成员或考核的补充记录 |
| `notifications` | 待办和通知 |
| `audit_logs` | 关键操作审计日志 |

关键数据库约束：

- 一个成员最多有一条未结束的 `seat_assignments`；
- 一个固定工位最多有一条未结束的 `seat_assignments`；
- 一个成员原则上最多有一个 `ACTIVE` 考核；
- 周报任务应通过 `review_id + period_index` 唯一定位；
- 删除成员时优先逻辑删除，保留历史记录；
- 时间字段统一保存为数据库约定时区，前端按本地时区显示。

## 9. 页面清单

### 普通成员端

1. 登录页；
2. 首页/我的待办；
3. 工位地图；
4. 工位申请详情；
5. 我的工位；
6. 我的考核列表与详情；
7. 周报编辑/查看页；
8. 特殊情况登记页；
9. 个人资料页。

### 管理端

1. 管理概览：空闲工位、待审核申请、进行中考核、待决策考核；
2. 成员管理与成员档案；
3. 工位区域与工位管理；
4. 工位申请审核；
5. 考核列表、发起考核和考核详情；
6. 到期考核汇总与决策页；
7. 审计日志页。

## 10. 接口范围示例

无需严格照抄路径，但应覆盖下列能力：

- `/api/auth/login`
- `/api/members`、`/api/members/{id}`、`/api/members/{id}/status`
- `/api/seat-areas`、`/api/seats`、`/api/seats/map`
- `/api/seat-applications`、`/api/seat-applications/{id}/approve`、`.../reject`
- `/api/seat-assignments`、`.../{id}/release`
- `/api/reviews`、`/api/reviews/{id}`、`.../extend`、`.../cancel`
- `/api/reviews/{id}/summary`、`.../decision`
- `/api/report-tasks/my`、`/api/report-tasks/{id}/report`
- `/api/special-circumstances`
- `/api/notifications/my`
- `/api/audit-logs`

统一响应格式、分页格式、错误码和参数校验规则。业务异常必须返回明确中文消息，不能只返回 500。

## 11. 非功能要求

- 关键分配与释放逻辑必须有事务；
- 防止同一工位并发重复分配；
- 后端必须执行权限校验，不能只依赖前端隐藏按钮；
- 所有输入进行校验和基本 XSS 防护；
- 密码使用 BCrypt 等可靠算法；
- 列表接口支持分页，常用查询字段建立索引；
- 状态转换集中管理，避免任意接口直接修改状态值；
- 关键服务编写单元测试或集成测试；
- 提供初始化 SQL、示例数据、README 和启动说明；
- 前端提供加载、空状态、错误提示和二次确认；
- 桌面端优先，同时保证常用页面在手机上可基本使用。

## 12. 第一版明确不做

- 不做自动综合评分；
- 不由 AI 自动决定成员去留或是否释放工位；
- 不要求所有成员长期提交周报；
- 不做复杂打卡、GPS、人脸识别或门禁硬件接入；
- 不强制连接 GitHub/Gitee；
- 不做完整项目管理系统；
- 不做设备借用和服务器资源管理；
- 不做复杂可视化工位平面图编辑器；
- 不做多级审批流；
- 不在第一版加入微服务、消息队列等非必要复杂架构。

## 13. 验收标准

至少完成以下端到端场景：

1. 管理员创建工位，成员能看到空闲工位并提交申请；
2. 管理员审核通过后，该成员获得固定工位，其他成员不能再次申请；
3. 管理员能对固定工位成员发起 4 周考核，系统生成对应周报任务；
4. 成员能保存草稿、提交周报，逾期提交状态正确；
5. 到期后系统汇总报告数量、内容、特殊情况和历史信息；
6. 管理员选择“保留固定工位”时，工位和成员状态正确；
7. 管理员选择“转为流动成员”时，成员状态、工位状态和绑定历史同步更新；
8. 所有关键操作均能在审计日志中查询；
9. 普通成员无法访问管理员接口，也不能查看或修改他人的私有报告；
10. 并发申请或审核不会导致一个工位分配给两个人。

## 14. 建议开发顺序

1. 初始化前后端工程、数据库、登录和权限；
2. 完成成员、工位区域、工位及状态模型；
3. 完成工位地图、申请审核和绑定历史；
4. 完成考核、周报任务和结构化周报；
5. 完成到期汇总、人工决策和事务状态变更；
6. 完成特殊情况、通知和审计日志；
7. 补充测试、初始化数据、部署配置和 README。

每一阶段完成后先运行测试并向用户汇报，不要一次性生成大量未经验证的代码。

## 15. 给 Codex 的实施指令

1. 开始前先检查当前目录是否已有项目、`AGENTS.md`、README 和未提交修改；不要覆盖用户现有代码。
2. 如果当前目录为空，先向用户确认：项目名称、技术栈是否采用本文默认方案、包名、前端风格和文件存储方案。
3. 先输出简短实施计划和拟建目录，再开始编码。
4. 优先实现第一版闭环，不添加本文“明确不做”的功能。
5. 对业务规则有歧义时先提问，禁止自行发明计分规则或自动清退规则。
6. 每次修改后执行对应的编译、测试或前端构建，并修复由本次修改造成的问题。
7. 不随意删除或重构无关文件，不使用破坏性 Git 命令。
8. 最终交付时说明已完成内容、运行方式、默认账号、测试结果和仍待确认事项。

## 16. 编码前仍需用户确认的问题

以下问题不会影响本文业务框架，但会影响具体实现：

1. 是否确定使用 `Spring Boot + Vue 3 + MySQL`？
2. 工位布局是一张图片上标点，还是按房间/区域建立网格？第一版建议网格。
3. 账号由管理员创建，还是允许学生使用学号注册后等待审核？第一版建议管理员创建。
4. 周报正式提交后是否允许成员修改？建议允许在截止前撤回重提，并保留修改记录。
5. 是否已有可用的项目代码、数据库表或 UI 风格要求？
6. 是否需要在第一版实际上传附件，还是先只保存成果链接？第一版建议先保存链接。

