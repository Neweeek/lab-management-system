# 管理员材料查询与项目页更新

- 管理员侧栏新增“报告查询”，考察列表增加“查看周报”快捷入口。
- 查询支持成员姓名/学号、截止日期范围、提交状态、考察范围和分页；已结束/取消考察的历史记录不隐藏。
- 完整周报以只读文档呈现，明确区分草稿、未保存和历史格式异常；成果链接仅允许 HTTP/HTTPS。
- 新查询接口 `/api/admin/reports` 和 `/api/admin/reports/{taskId}` 强制检查管理员登录，普通成员不能访问。
- 项目页采用与现有工作台一致的深蓝、蓝色动作按钮与白色内容面板；列表展示容量，详情分为概况/团队/报名，报名支持按状态筛选。

验证：17 项后端测试通过；前端生产构建及三个运行时模板编译通过。本地浏览器使用模拟数据验证组件显示、报告筛选和阅读，检查桌面及390px布局。生产数据未用于开发测试。

本次无数据库结构变更。上线仍需服务器执行更新脚本并进行在线管理员验收，不能仅凭本地构建认定线上已更新。
